import React from 'react'

export interface IWalletInstallProps {}

const WalletInstall: React.FunctionComponent<IWalletInstallProps> = props => {
    return <div>You don't have metamask install yet, please install</div>
}

export default WalletInstall